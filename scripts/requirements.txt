# Python deps for scripts/build-depth-maps.py (the depth-map pipeline).
#
# Use Python 3.11 or 3.12 — NOT whatever `python3` happens to be. onnxruntime
# lags new releases by a long while, and on 3.14 `python3 -m venv` can't even
# bootstrap pip (ensurepip exits 1).
#
# Install into a throwaway venv so nothing touches system Python:
#   python3.12 -m venv .venv-depth
#   ./.venv-depth/bin/pip install -r scripts/requirements.txt
#   ./.venv-depth/bin/python scripts/build-depth-maps.py
numpy
onnxruntime
pillow
